<div class="min-h-screen bg-gradient-to-b from-indigo-50 to-gray-100 flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-lg w-full bg-white rounded-xl shadow-2xl p-8 space-y-8">
    <div class="text-center">
      <h1 class="text-3xl font-extrabold text-gray-900">Skincare Analysis</h1>
      <p class="mt-2 text-sm text-gray-600">Upload a selfie and provide your email and we'll send over your skin analysis along with a personalized skincare and diet plan with recommendations.</p>
    </div>

    <%= form_with model: @skincare_analysis, local: true, class: "space-y-6" do |form| %>
      <% if @skincare_analysis.errors.any? %>
        <div class="bg-red-50 border-l-4 border-red-400 p-4 rounded-md">
          <p class="text-sm text-red-700">
            <strong class="font-semibold">Please fix the following errors:</strong>
            <ul class="list-disc pl-5 mt-2">
              <% @skincare_analysis.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </p>
        </div>
      <% end %>

      <div>
        <%= form.label :email, "Email Address", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= form.email_field :email, 
            class: "block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm", 
            placeholder: "Enter your email" %>
      </div>

      <div>
        <%= form.label :image_url, "Choose your selfie", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <div class="flex items-center justify-center w-full">
          <%= form.file_field :image_url, 
              accept: "image/jpeg,image/png", 
              class: "block w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-md file:border-0 file:text-sm file:font-semibold file:bg-indigo-50 file:text-indigo-700 hover:file:bg-indigo-100 cursor-pointer" %>
        </div>
      </div>

      <div>
        <%= form.submit "Analyze My Selfie", 
            class: "w-full flex justify-center py-3 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition duration-150 ease-in-out cursor-pointer" %>
        <p class="text-xs text-gray-500 mt-2 text-center">
          Please check your spam folder if you don't see your analysis in your inbox.
        </p>
      </div>
    <% end %>
  </div>
</div>